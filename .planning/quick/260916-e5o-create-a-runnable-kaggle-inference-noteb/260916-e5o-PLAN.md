---
phase: quick-260916-e5o
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Diff_ICMH_Kaggle_Quick_Run.ipynb
autonomous: true
requirements:
  - PRE-01
  - EVAL-08
  - REPT-04
estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 1
  confidence: low
must_haves:
  truths:
    - "A Kaggle user is stopped with an actionable message before downloads when GPU, Internet access, or working-disk capacity is unavailable."
    - "The default path selects BPP_WEIGHT=2, stages only kodim01.png, and invokes inference_partition.py with 10 diffusion steps; the notebook documents switching to the README-quality 50-step path."
    - "The notebook acquires the SD 2.1, RAM++, and selected Diff-ICMH checkpoints at the paths consumed by configs/model/diffeic.yaml and the inference CLI."
    - "After a successful Kaggle run, the notebook shows the original and reconstruction, reports bpp plus PSNR/SSIM/LPIPS metrics, and produces a downloadable ZIP containing the outputs."
    - "Local verification proves notebook JSON validity, Python AST validity for every code cell, and static checkpoint/config/CLI wiring without claiming that Kaggle execution occurred."
  artifacts:
    - path: "Diff_ICMH_Kaggle_Quick_Run.ipynb"
      provides: "Ordered Kaggle GPU inference workflow from preflight through ZIP export"
      contains: "inference_partition.py"
  key_links:
    - from: "Diff_ICMH_Kaggle_Quick_Run.ipynb configuration cell"
      to: "Hugging Face checkpoint download cell"
      via: "BPP_WEIGHT-derived FOLDER_NAME selects exactly one Diff-ICMH model checkpoint"
      pattern: "difficmh_models/.*/model\\.ckpt"
    - from: "Diff_ICMH_Kaggle_Quick_Run.ipynb inference cell"
      to: "inference_partition.py"
      via: "argument-list subprocess call passes both checkpoints, model config, input/output directories, step count, device, and README dotlist overrides"
      pattern: "inference_partition\\.py.*--ckpt_sd.*--ckpt_lc.*--config"
    - from: "Diff_ICMH_Kaggle_Quick_Run.ipynb inference outputs"
      to: "Diff_ICMH_Kaggle_Quick_Run.ipynb visualization/export cells"
      via: "OUTPUT_DIR supplies reconstructed PNGs, data bitstreams, bpp.txt, and the ZIP root"
      pattern: "OUTPUT_DIR"
---

<objective>
Turn the existing Diff-ICMH notebook into a self-contained Kaggle inference path that follows the repository's actual `inference_partition.py` interface and the current project runtime constraints.

Purpose: Give the project a reproducible one-image GPU smoke test before larger evaluation or training work consumes compute units.
Output: An updated `Diff_ICMH_Kaggle_Quick_Run.ipynb` with preflight, compatible dependency setup, selective checkpoint acquisition, inference, visualization, metrics, export, and local structural validation.
</objective>

<execution_context>
@D:/Diff_ICMH/.claude/gsd-core/workflows/execute-plan.md
@D:/Diff_ICMH/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@README.md
@inference_partition.py
@configs/model/diffeic.yaml
@requirements.txt
@utils/metrics.py
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
`inference_partition.py` accepts `--ckpt_sd`, `--ckpt_lc`, `--config`, `--input`, `--output`, `--sampler`, `--steps`, `--seed`, `--device`, optional `--anchor_prior_strength`, and trailing OmegaConf dotlist overrides. It loads `state_dict` from both checkpoint files, merges them, processes every image in the input directory, writes reconstructed PNGs and bitstreams under the output directory, and emits `bpp.txt` containing per-image and average bpp/PSNR/SSIM/LPIPS values.

`configs/model/diffeic.yaml` expects the SD checkpoint at `checkpoints/sd2p1/v2-1_512-ema-pruned.ckpt`; `TagGCM` expects RAM++ at `checkpoints/ram/ram_plus_swin_large_14m.pth`. The README command additionally requires `params.control_stage_config.params.control_model_ratio`, `params.preprocess_tag_config.params.enabled=True`, and `params.c_cfg_scale` dotlist overrides.

The repository revision observed during planning is `aebaff6f61d0253e09e3f482d5887aa50e0a539a` on `origin/main`; use it as the notebook's default `REPO_REF` so Kaggle runs do not silently drift from the code this plan inspected.
</interfaces>

Hugging Face is dependency acquisition for model files in this quick task, not a product API integration. Do not add an API coverage matrix, authentication flow, or schema/database work.
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Deliver the one-image Kaggle inference path end to end</name>
  <files>Diff_ICMH_Kaggle_Quick_Run.ipynb</files>
  <action>Update the existing notebook in place and retain nbformat 4 with Python 3 metadata, empty execution counts, and empty saved outputs. Organize its markdown and code cells in runnable top-to-bottom order: usage instructions; a preflight that checks `/kaggle/working`, `torch.cuda.is_available()`, detected Python/Torch/CUDA/GPU versions, at least 15 GiB working space, and HTTPS reachability of both GitHub and Hugging Face via `urllib.request.urlopen` before cloning or downloading; repository acquisition from the existing GitHub remote followed by checkout of default `REPO_REF=aebaff6f61d0253e09e3f482d5887aa50e0a539a`; dependency installation and import checks; checkpoint acquisition; one-image staging; CLI inference; result inspection; and ZIP export.

For dependencies, preserve Kaggle's preinstalled Torch/Torchvision/CUDA instead of replacing that stack. Install `numpy>=1.26,<2.0`, unified `lightning>=2.6`, `compressai==1.2.8` without dependency-driven Torch replacement, `pyiqa==0.1.15.post2`, the remaining inference imports, the vendored RAM++ package with `--no-deps`, and an xFormers wheel selected from the PyTorch wheel index for the detected Torch/CUDA ABI; fail with the detected versions and a corrective message when an ABI-compatible wheel/import cannot be established. Because the inspected inference sources still import legacy Lightning utility paths and `utils.metrics` imports the retired standalone LPIPS surface, create compatibility modules only under `/kaggle/working/difficmh_compat` and prepend that directory to the inference subprocess `PYTHONPATH`: expose the type/rank-zero aliases required by `model.diffeic` and `ldm.models.diffusion.ddpm`, and expose the `lpips.LPIPS(net=...).to(...)(img1, img2, normalize=...)` call shape through pyiqa. Do not rewrite repository source files in the notebook. Run a subprocess import smoke check for `inference_partition` after installation and before the multi-gigabyte checkpoint downloads.

Download through `huggingface_hub` over verified HTTPS, never a certificate-bypass command: SD 2.1 from `Manojb/stable-diffusion-2-1-base` into `checkpoints/sd2p1/v2-1_512-ema-pruned.ckpt`, RAM++ from `xinyu1205/recognize-anything-plus-model` into `checkpoints/ram/ram_plus_swin_large_14m.pth`, and only `difficmh_models/{FOLDER_NAME}/model.ckpt` from `RuoyuFeng/Diff-ICMH`. Assert each expected file exists and exceeds a non-trivial size before inference. Set `BPP_WEIGHT = 2`, derive the README folder name, set `SMOKE_TEST = True`, and define `STEPS = 10 if SMOKE_TEST else 50`; explain in markdown that 10 steps is the one-image smoke run and 50 steps is the README-quality setting. Select `data/kodak_subset/kodim01.png` explicitly for the smoke run, while leaving a documented `SOURCE_DIR`/`MAX_IMAGES` path for Kaggle datasets and multi-image runs.

Build the inference command as an argument list and call it with `subprocess.run(..., check=True, cwd=REPO_DIR, env=env)`, never through a shell string. Pass explicit `--ckpt_sd`, `--ckpt_lc`, `--config configs/model/diffeic.yaml`, staged input/output directories, `--steps`, `--seed 231`, `--device cuda`, and the three README dotlist overrides for control-module ratio, tag preprocessing, and CFG scale. After success, assert the reconstructed PNG, non-empty bitstream directory, and `bpp.txt`; display original and reconstruction side by side; print the CLI metric file; recompute/display PSNR, SSIM, and LPIPS through pyiqa for the staged pair; and create a downloadable ZIP rooted at `OUTPUT_DIR`. State plainly in the notebook that local validation covers structure and wiring only and that Kaggle end-to-end status remains unverified until Run All completes there.</action>
  <verify>
    <automated>python -c "import ast,json,pathlib; p=pathlib.Path(r'Diff_ICMH_Kaggle_Quick_Run.ipynb'); nb=json.loads(p.read_text(encoding='utf-8')); cells=[c for c in nb.get('cells',[]) if c.get('cell_type')=='code']; [ast.parse(''.join(c.get('source',[])), filename=f'cell-{i}') for i,c in enumerate(cells)]; code='\n'.join(''.join(c.get('source',[])) for c in cells); required=['torch.cuda.is_available','urlopen','github.com','huggingface.co','aebaff6f61d0253e09e3f482d5887aa50e0a539a','Manojb/stable-diffusion-2-1-base','v2-1_512-ema-pruned.ckpt','xinyu1205/recognize-anything-plus-model','ram_plus_swin_large_14m.pth','RuoyuFeng/Diff-ICMH','difficmh_models','BPP_WEIGHT = 2','STEPS = 10 if SMOKE_TEST else 50','kodim01.png','inference_partition.py','--ckpt_sd','--ckpt_lc','--config','configs/model/diffeic.yaml','--input','--output','--steps','--seed','--device','params.control_stage_config.params.control_model_ratio','params.preprocess_tag_config.params.enabled=True','params.c_cfg_scale','bpp.txt','pyiqa','matplotlib','make_archive']; missing=[token for token in required if token not in code]; assert nb.get('nbformat')==4, nb.get('nbformat'); assert cells, 'no code cells'; assert all(c.get('execution_count') is None and not c.get('outputs') for c in cells), 'saved execution state present'; assert not missing, f'missing wiring: {missing}'; print(f'validated notebook JSON, {len(cells)} Python cells, and {len(required)} wiring tokens')"</automated>
    <human-check>On Kaggle, enable Accelerator=GPU and Internet, upload/open the notebook, and choose Run All. A successful live check reaches the final cell with one `kodim01` reconstruction, non-empty bitstream data, printed bpp/PSNR/SSIM/LPIPS values, and a downloadable ZIP. Record Kaggle end-to-end success only after this live check actually completes.</human-check>
  </verify>
  <done>`Diff_ICMH_Kaggle_Quick_Run.ipynb` passes the local JSON/AST/static-wiring command, contains the complete one-image path with BPP_WEIGHT=2 and 10-step defaults plus documented 50-step use, and makes no unsupported claim that Kaggle execution was performed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Kaggle runtime -> GitHub/PyPI/PyTorch/Hugging Face | The notebook acquires executable packages, repository code, and model files from external hosts. |
| Kaggle input -> inference process | User-selected images and paths enter a GPU subprocess and determine created output paths. |
| Kaggle working directory -> downloadable ZIP | Generated images, bitstreams, and metrics are packaged for download. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-260916-e5o-01 | Tampering | Repository checkout | high | mitigate | Default to the planning-observed commit, display the checked-out commit, and use argument-list subprocess calls rather than shell interpolation. |
| T-260916-e5o-02 | Tampering | Checkpoint downloads | high | mitigate | Use `huggingface_hub` over normal TLS with exact repository IDs/selective paths, then assert expected filenames and non-trivial sizes before model loading. |
| T-260916-e5o-03 | Denial of Service | Disk/VRAM/runtime budget | medium | mitigate | Gate downloads on GPU and free-space checks, default to one known image and 10 steps, and surface detected runtime versions on failure. |
| T-260916-e5o-04 | Information Disclosure | ZIP export | low | accept | The archive is built only from the dedicated output directory; the notebook does not upload inputs, credentials, or unrelated Kaggle files. |
| T-260916-e5o-SC | Tampering | pip/xFormers dependency acquisition | high | mitigate | Preserve preinstalled Torch, use named PyPI/PyTorch indexes and researched package identities, constrain versions, prevent CompressAI/RAM++ from replacing Torch transitively, and require post-install version/import checks before downloads or inference. |
</threat_model>

<verification>
Run the task's automated command from `D:/Diff_ICMH`; it must parse the notebook as JSON, parse every Python code cell with `ast`, reject stored outputs/execution counts, and confirm all checkpoint/config/CLI/output wiring tokens. Treat a later Kaggle Run All as separate runtime evidence, not as implied by local success.
</verification>

<success_criteria>
- The requested notebook is the only implementation artifact modified.
- GPU, Internet, disk, and ABI/dependency failures stop before expensive checkpoint downloads with actionable diagnostics.
- The smoke configuration is BPP_WEIGHT=2, `kodim01.png`, and 10 steps; 50 steps is clearly documented as the README-quality path.
- Checkpoint destinations and the inference command match the interfaces in `inference_partition.py` and `configs/model/diffeic.yaml`.
- Visualization, bpp/PSNR/SSIM/LPIPS reporting, bitstream checks, and ZIP export are wired to the actual output directory.
- Local validation passes without representing an unperformed Kaggle execution as complete.
</success_criteria>

## Source Coverage Audit

| SOURCE | ID | Feature/Requirement | Task | Status | Notes |
|--------|----|---------------------|------|--------|-------|
| GOAL | quick-260916-e5o | Runnable Kaggle inference notebook with preflight through export | 1 | COVERED | One vertical notebook path. |
| REQ | PRE-01 | Current-runtime compatible dependency setup | 1 | COVERED | Preserves Torch and validates ABI/imports. |
| REQ | EVAL-08 | Image-quality metrics through pyiqa | 1 | COVERED | Post-run PSNR/SSIM/LPIPS reporting. |
| REQ | REPT-04 | Reproducible smoke test artifact | 1 | COVERED | Pinned repo revision, fixed defaults, structural validator. |
| RESEARCH | stack-runtime | Keep preinstalled Torch; use Lightning 2.x, current CompressAI/pyiqa, matched xFormers | 1 | COVERED | Notebook-local compatibility seam avoids repository edits. |
| RESEARCH | checkpoints | SD 2.1, RAM++, and one selected Diff-ICMH checkpoint | 1 | COVERED | Selective Hugging Face acquisition. |
| CONTEXT | prompt-target | Update the existing notebook, not a second file | 1 | COVERED | Single implementation artifact. |
| CONTEXT | prompt-defaults | BPP_WEIGHT=2 and 10-step smoke; document 50 steps | 1 | COVERED | Exact defaults and markdown guidance. |
| CONTEXT | prompt-verification | JSON + AST + static wiring locally; no false Kaggle E2E claim | 1 | COVERED | Automated command plus explicit human live check. |
| CONTEXT | prompt-boundary | Hugging Face is dependency acquisition; no API matrix or schema changes | 1 | COVERED | Scope boundary stated in context. |

<output>
Create `.planning/quick/260916-e5o-create-a-runnable-kaggle-inference-noteb/260916-e5o-SUMMARY.md` when execution is complete.
</output>
